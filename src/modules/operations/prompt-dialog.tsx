import { X } from "@phosphor-icons/react";
import type { PromptScope } from "./types";

export function PromptDialog({ scope, value, saving, error, onChange, onClose, onSave }: { scope: PromptScope; value: string; saving: boolean; error: string; onChange: (value: string) => void; onClose: () => void; onSave: () => void }) {
  const session = scope === "session";
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/35 p-4" onMouseDown={onClose}><section role="dialog" aria-modal="true" aria-labelledby="prompt-dialog-title" onMouseDown={(e) => e.stopPropagation()} className="w-full max-w-2xl rounded-xl bg-surface shadow-2xl"><header className="flex items-center justify-between border-b border-border px-5 py-4"><div><h2 id="prompt-dialog-title" className="font-semibold">{session ? "会话级 Prompt" : "用户级 Prompt"}</h2><p className="mt-1 text-xs text-text-muted">{session ? "只影响当前会话" : "影响该用户与当前 Agent 的所有会话"}</p></div><button type="button" onClick={onClose} aria-label="关闭 Prompt 编辑" className="p-2"><X size={18} /></button></header><div className="p-5"><textarea autoFocus value={value} onChange={(e) => onChange(e.target.value)} rows={12} placeholder="输入 Prompt 补丁；留空表示移除补丁" className="w-full resize-y rounded-lg border border-border bg-surface p-3 text-sm leading-6 outline-none focus:border-primary" />{error && <p className="mt-3 text-sm text-danger">{error}</p>}</div><footer className="flex justify-end gap-3 border-t border-border px-5 py-4"><button type="button" onClick={onClose} className="button-secondary">取消</button><button type="button" onClick={onSave} disabled={saving} className="button-primary">{saving ? "保存中…" : "保存补丁"}</button></footer></section></div>;
}
