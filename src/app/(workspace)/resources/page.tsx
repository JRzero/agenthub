import { FutureModulePage } from "@/shared/ui/future-module-page";

export default function ResourcesPage() {
  return <FutureModulePage eyebrow="工作空间" title="资源库" description="技能、知识、媒体和模板会在后续纵切从旧 Creator 分域迁移，并保持现有 API 兼容。" />;
}
