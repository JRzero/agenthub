import { FutureModulePage } from "@/shared/ui/future-module-page";

export default function ResourcesPage() {
  return <FutureModulePage eyebrow="工作空间" title="资源" description="管理可被 Agent 复用的技能与知识，并保持现有 API 兼容。" />;
}
