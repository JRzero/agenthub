import { FutureModulePage } from "@/shared/ui/future-module-page";

export default function WorkbenchPage() {
  return <FutureModulePage eyebrow="工作空间" title="工作台" description="工作台将聚合真实待办、最近资产和跨 Client 表现；当前纵切先从 Agent Asset 工作区建立可靠数据边界。" />;
}
