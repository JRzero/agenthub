## Context

The root route currently performs a server redirect to `/workbench`, while authenticated workspace pages live under the `(workspace)` route group and are protected by `WorkspaceShell`. The approved V4 reference defines a public editorial landing page with a dark product-proof surface, continuous product states, a sticky creation narrative, creator scenarios, and an intent-to-auth handoff. The implementation must use existing dependencies and must not alter backend, authentication, invitation, or workspace behavior.

## Goals / Non-Goals

**Goals:**

- Make `/` publicly browsable and closely match the V4 desktop composition while adapting cleanly to 390 px.
- Deliver working anchor navigation, product-state selection, scroll-driven five-step storytelling, and creation-intent interaction.
- Preserve anonymous intent only in browser session storage and route continuation through existing authenticated entry points.
- Meet keyboard, focus, semantic HTML, and reduced-motion expectations.
- Keep public-site styles and behavior isolated from workspace modules.

**Non-Goals:**

- No anonymous Agent generation, server persistence, publishing, API/SDK promises, marketplace, metrics, endorsements, or Living World content.
- No backend, auth protocol, invitation, request-header, dependency, lockfile, or project-configuration changes.
- No deployment or production asset-licensing decision.

## Decisions

1. **Isolate the landing experience in `src/modules/landing/`.** The root page imports a dedicated public-site component and CSS module. This avoids changing global workspace layout semantics and prevents V4 styling from leaking into authenticated surfaces. Keeping the implementation directly in `src/app/page.tsx` was rejected because the interactions and responsive states are substantial.
2. **Use supplied and existing raster assets.** The approved V4 design reference supplies the visual target, while the existing AgentHub logo and approved generated concept source supply visible imagery. UI detail is implemented as semantic native interface panels using the product's actual capability vocabulary; no hand-drawn SVG, CSS illustration, or placeholder imagery is introduced.
3. **Keep intent ephemeral and local.** A client component writes the trimmed intent to a namespaced `sessionStorage` key only when the visitor explicitly continues. It then navigates to `/login?next=/assets/create` or `/register?next=/assets/create`. Session storage was chosen over local storage to minimize persistence; direct API submission was rejected because no generation endpoint is defined.
4. **Use explicit state controls plus progressive scroll enhancement.** Product states are buttons/tabs and the five-step narrative uses native buttons with a requestAnimationFrame-scheduled geometry check on desktop. The closest chapter to a stable viewport focus line becomes active, while a short manual-selection hold prevents scroll feedback from immediately overriding a click. This keeps all content operable without animation and supports keyboard selection. Reduced-motion disables automatic state changes and smooth scrolling and reveals the stable default state.
5. **Preserve route protection structurally.** Only the root route changes. Existing workspace pages remain within `(workspace)` and continue to be guarded by `WorkspaceShell`; login and registration routes remain under `(auth)`.
6. **Scope visual evidence to local QA.** The V4 target and storyboard are copied into `docs/qa/design-reference/`; browser captures and comparison reports live under the existing QA directories.
7. **Treat the first acceptance feedback as a higher-priority V4 refinement.** Decorative outer frames, repeated equal cards, and continuous section dividers are removed from the public narrative. Necessary borders remain inside the truthful product UI and input control, while product tabs become a text track, the five steps become an open chapter flow, and creator scenarios become an asymmetric editorial collage.
8. **Keep state transitions composited and height-stable.** Product states share a fixed grid stack and transition only opacity and transform, so rapid selection cannot flash or change container height. Desktop creation chapters keep a stable footprint and reveal the active preview with opacity and transform; mobile shows one stable active detail. All motion uses a shared restrained rhythm and becomes immediate with no displacement under reduced motion.
9. **Adopt the selected Living Blueprint as the new visual truth.** The public site uses a warm paper field, editorial Songti hierarchy, coral action language, a raster hand-drawn growth path, raster print registration marks, and one angled dark AgentHub product proof. The product proof continues to use native, capability-accurate UI and is explicitly identified as a demo interface rather than rasterizing or copying the concept image's fictional product state.
10. **Recompose mobile instead of scaling the desktop hero.** At 390 px the headline, two-column milestone ledger, raster growth path, and simplified build proof form separate vertical beats. The product navigation is reduced to real Agent Asset scopes and the chat proof is omitted from the hero crop, while the full interactive product state remains available in the following section.
11. **Treat the user-marked visual revision as the highest-priority Hero rule.** The repeated header creation CTA and the entire angled Hero workbench proof are removed at every breakpoint. The Hero keeps one primary creation CTA, the raster growth path, all four stage annotations, and an editorial paper field; the following `#product` section remains the single truthful interactive product proof.
12. **Adopt the approved dark management references as the latest visual truth.** The public site moves from warm paper and coral to a near-black cinematic field, fluorescent lime accents, strong white Chinese typography, asymmetric raster portrait placement, and deliberately sparse borders. Earlier Living Blueprint paper, print marks, and coral action styling are superseded, while the creator-first story, truthful product proof, and existing routes remain intact.
13. **Reuse workbench carousel behavior for the public role-asset showcase.** The landing carousel imports the existing workbench transition and autoplay helpers instead of creating a second timing model. It supports previous/next, side-card focus, a three-second advance, and pauses while hovered, focus-within, hidden, reduced-motion, or transitioning. Public cards use reviewed static/demo imagery and identify themselves as examples; they do not claim live asset counts, activity, or performance.
14. **Compose mobile independently.** At 390 px the Hero image layers become a compact vertical cinematic composition and the asset carousel exposes one dominant card plus touch-safe controls and adjacent selections. Desktop transforms are not mechanically scaled, and no section may create horizontal document overflow.
15. **Adopt the live reference's full-page composition, not only its palette.** The public narrative is ordered as transparent navigation over a full-height Hero, the approved layered role carousel, one long-scroll five-stage product stage, three equal cinematic scenario images, a horizontal intent handoff, and a flat footer. The former standalone product proof and lime creation-flow ledger are merged into the sticky stage so headings and capability claims are not repeated. Desktop progress maps the stage container's scroll travel to five deterministic states; mobile removes sticky positioning and keeps one manually selectable product window above the full stage selector.
16. **Use the fresh R5 live recapture for whole-page proportions.** The desktop reference measures a 948 px framed Hero, compact asset transition, 4600 px sticky flow, 192 px three-image scenario strip, 440 px centered intent handoff, and 70 px footer at 1440×1000. AgentHub mirrors that large-scale rhythm except where the user-approved 图 1 layered role carousel intentionally needs a taller exhibition surface. At 390×844, the Hero remains one viewport tall, flow becomes a product panel followed by explicit stage buttons, scenarios stack, and the footer collapses to brand plus copyright. Reference-only metrics, management copy, pricing, documentation, customer claims, and fabricated product-state values remain excluded.
17. **Keep role status accessible without duplicating the focused card.** The visible current-role summary beneath the controls is removed. The existing counter owns the polite atomic label for the focused role, so screen-reader feedback remains available without a duplicate visible content block. The focus card is reduced from 330×500 to 280×410 on desktop and from 248×365 to 212×300 on mobile, with proportional stage and slot offsets; transition timing, depth scales, progress, and pause behavior remain unchanged.
18. **Keep creation stages in the formal product flow only.** The Hero-only four-stage trail and its lower supporting portraits are removed from DOM/CSS to avoid duplicating the complete five-stage `#product` interaction. The Hero height and remaining text/portrait geometry are tightened at desktop and mobile widths while navigation, the single creation CTA, and downstream sections remain unchanged.
19. **Treat section seams as one continuous editorial system.** The shortened Hero tucks over the role-assets surface, role assets hand directly into the five-stage flow without an unowned spacer, and the flow closes into a readable desktop scenario band before the intent/footer close. A landing-scoped 1320px content rail, shared subtle boundary token, shared cinematic shadow, and 48px primary-action height align sections without changing the approved 280×410 and 212×300 role cards or restoring removed Hero nodes.
20. **Use one coherent perspective system for the Hero character matrix.** The Hero's right side uses five individually reviewed raster character studies with a shared yaw and clockwise tilt: one black-background cinematic strategist with purple/blue rim light at the visual center, plus cel-anime, game-concept, robotic, and painterly-fantasy examples receding around it. The array begins after the desktop copy safety boundary, drops to one primary plus three supporting cards at 390 px, and never restores green atmosphere over the left copy field or implies that these brand studies are live assets.
21. **Treat the Hero imagery as a cropped cinematic wall rather than a bounded stage.** The supplied R10 reference supersedes the sparse five-card perspective geometry: eleven reused-raster slots occupy a three-column/multirow background layer from roughly one quarter of the viewport, all cards use one flat 6° clockwise axis, and the enlarged focal card spans two rows inside the same occlusion system. A neutral black visibility mask protects the copy field; luminance and crop create depth without `rotateY`, decorative atmosphere or a CSS halo. At 390 px the wall reduces to one focal plus four staggered backgrounds while preserving overflow containment. Character images remain labelled brand examples, not live assets.
22. **Supersede the R10 flat-axis assumption with one preserved two-level 3D system.** The Hero figure establishes real perspective, the eleven existing cards share a single subtly pitched/yawed/clockwise parent plane, and the focal card inherits that plane before a restrained local `translateZ` and counter-pitch/yaw/clockwise rotation lift its upper-right corner toward the viewer. Stage, plane, and focal card preserve 3D so the lift is not flattened; the copy-protection treatment is a neutral overlay outside the transformed subtree. A soft lower-left black shadow expresses elevation without a colored halo. Desktop focal height increases to 730px inside the frozen 820px Hero, while mobile uses the same hierarchy at reduced X/Y/Z amplitudes and keeps the frozen 760px Hero free of document overflow.
23. **Treat the supplied R12 Hero composite as the final raster truth.** The new text-free 1449×1086 source supersedes decisions 20–22 for Hero card geometry: it is one finished two-dimensional masonry composite with a baked black negative field, card projection, overlap, outline, crop, and shadow. The Hero renders that exact supplied file as one decorative image and contains no reconstructed card slots, perspective stage, rotateX/rotateY, translateZ, focal-card DOM, CSS halo, or generated replacement. Desktop right-aligns the raster at the frozen 820px Hero height; mobile reuses the same raster with one lower-Hero crop inside the frozen 760px frame. Earlier R9 role files remain in the repository but are no longer consumed by the Hero.
24. **Reconstruct the R13 Hero from independent two-dimensional role cards.** This supersedes decision 23's runtime-composite choice while keeping its 1449×1086 image only as geometry evidence. A typed twelve-slot array supplies id, independent R9 raster source, alt, slot, z-index, luminance tier, crop and scale; the DOM renders one image/container pair per slot. One 1449:1086 coordinate stage and a shared `skewX(-10deg) rotateZ(2deg)` token reproduce the visible masonry envelope, overlap and edge crops without a sprite, canvas, background slice, perspective, rotateX/rotateY or translateZ. Mobile retains the independent nodes and displays a six-card subset inside the frozen lower Hero crop.
25. **Give the creation-intent close a deliberately spacious reading rhythm.** The R14 revision changes only the intent section's title markup and vertical spacing. Its two semantic title lines become explicit block spans on desktop, use a 1.18 display leading plus a restrained inter-line gap, and sit within a wider centered measure; mobile permits the same two spans to wrap naturally into two or three visible lines. Kicker, subtitle, existing input control, suggestions, privacy metadata, routes, behavior, color, imagery, Footer, and every upstream section remain unchanged.
26. **Size the R15 desktop Hero role stage from viewport width.** The twelve-card R13 coordinate stage keeps its slots, crops, shared two-dimensional matrix, z-index, imagery, and DOM unchanged, but desktop sizing moves from a fixed Hero-height-derived 1094px width to `clamp(1094px, 76vw, 1600px)`. The stage preserves 1449:1086, is vertically centered, and lets the frozen 820px Hero crop excess top and bottom at wide viewports. The existing 390px stage explicitly resets the desktop translation and retains its exact 560×420 position and six visible cards.
27. **Keep the R16 initial intent form deliberately concise.** The explanatory sentence below the heading and the complete counter/privacy metadata row below the suggestions are removed from the initial form DOM rather than visually hidden. The heading now hands directly to the unchanged input after 54px on desktop and 48px on mobile; the unchanged suggestion row remains 26px below the control, followed by a compact 42–49px section close. Validation, submission, session-only storage, login/invitation continuation, and the post-submit disclosure remain unchanged, so the truthful persistence boundary is still shown when the visitor explicitly submits.
28. **Treat the supplied 3006×1468 R17 image as the final Hero-only geometry truth.** Normalize it to the source's Retina CSS viewport of 1503×734, then match the full-height black frame, 130px content rail, header baseline, exact three-line management headline, description, CTA, proof rail, and dense right-side wall. The runtime wall remains twelve independent image/card nodes with one shared `skewX(-12deg) rotateZ(1deg)` matrix; the focal card measures approximately 591×559 at x764/y47 in the normalized viewport. Two independently generated black-background character studies fill the missing rounded-fantasy and alpaca roles, while the other eight unique images reuse existing R9/demo assets. The reference's unverified 800+/40K+/15K+/27K+ claims are deliberately replaced by `05 / DEMO / LIVE` capability-boundary labels, and every reference-like navigation label still points to an existing AgentHub anchor or authentication route. R16 and every section below the Hero remain unchanged.
29. **Make R17 source fidelity a blocking requirement, not a geometry-only pass.** The focal slot uses the user-directed `role-creative-director-demo.png` with a warm-lit black technical crop calibrated to `50% 16% / scale(1.46)` instead of the earlier purple-rim portrait. All twelve slots use distinct independent sources, and reference positions preserve the semantic distribution of glasses strategist, orange-haired game host, headset woman, senior scientist, rounded fantasy character, game architect, robot, alpaca, young operator, silver-haired fantasy woman, edge fantasy role, and focal creative director. Four missing supporting roles are generated as separate 1024×1536 black-scene rasters with restrained lime details and no text/logo/watermark. R16, downstream sections, routes, and the shared two-dimensional wall geometry remain frozen.
30. **Use the supplied focal-card pixels when a merely similar portrait still fails R17.** Pass 4 supersedes decision 29's focal source only: measure the four card-edge intersections in the 3006×1468 reference, perform one source-to-rectangle homography at original 2× density, inset past the source border/rounded-corner overlap, and save the 1200×1420 card interior as `hero-main-reference-r18.png`. The runtime consumes it through the existing main card's independent DOM/Image node at `50% 50% / scale(1)` so the shared wall matrix is applied exactly once. The extraction may contain only the focal person and black card interior—never Hero copy, navigation, metrics, neighboring cards, or the complete wall—and is restricted to local acceptance until public-use rights are confirmed.
31. **Make the intent input one structural capsule.** The R19 correction assigns the background, 1px outline, responsive height, radius, hover, and focus-within treatment to `.intentControl`. Its textarea becomes a transparent borderless child with explicit right-side text clearance, while the unchanged circular submit action is absolutely centered with `top: 50% / translateY(-50%)` and an 8–12px right inset. The wrapper does not clip focus rings, and all four button edges remain inside the control at 1510×561 and 390×844 without changing form behavior, section geometry, suggestions, or downstream content.
32. **Keep the R20 role exhibition concise and consistently rounded.** The left intro uses the exact two-line semantic title `让角色管理` / `更清晰、更高效。` within its existing desktop/mobile envelope, so the section height, carousel alignment, and downstream seam do not move. Every role-card article owns one 18px desktop / 14px mobile clipping boundary; the image and side-select layers inherit it, and the copy overlay repeats the matching lower corners. The active lime border remains the article border with no halo. Slot size, overlap, scale, transform, z-index, transition, controls, progress, autoplay, keyboard, and reduced-motion behavior are frozen.
33. **Treat R21 as a Hero handoff-emphasis annotation, not a redesign.** The desktop Hero CTA grows from 116×44 / 13px to 148×52 / 14px while preserving its existing lime surface, 9px radius, hover/focus behavior, semantic link, and `/login?next=%2Fassets%2Fcreate` destination. The proof group's CTA-to-status gap grows from 24px to 38px and its internal status-to-stats gap remains 29px. At 390px the CTA grows from 112×46 / 12px to 126×48 / 13px, the CTA-to-status gap grows from 16px to 25px, and the internal 20px gap remains fixed. The header login control, copy, character wall, Hero height, and every downstream section are frozen.
34. **Map the R22 document suggestions to truthful lifecycle language.** The flow heading becomes the two-line `一个 Agent， / 从创建到运营` with its existing lime emphasis and the concise lifecycle sentence `角色、知识、测试、发布、迭代，完整流程统一管理。`. The use-case band becomes `覆盖 Agent / 全生命周期` and each image carries only its number, audience title, and one complete untruncated value sentence. The intent heading uses `快速创建、测试与管理 Agent， / 让每一个角色的运营更简单。` instead of the document's proposed search wording because the existing control organizes a creation intent and does not search assets. Its three shortcuts share an 11px rounded hover/focus-visible contract. Hero R18/R21, role assets R20, images, stage content, intent submission and authentication continuation remain frozen.
35. **Curate the public role exhibition as five honest brand examples.** The R23 role set uses the supplied independent close-portrait rasters for 墨衡、知序、沐橙、澄音、拓野, losslessly encoded as 1122×1402 WebP assets through the repository-installed Sharp runtime. Every card pairs one approved role title, complete description and capability focus with the explicit `品牌示例` boundary; none imply live status or operating data. The existing five-card geometry, 18px/14px clipping, workbench transition, three-second autoplay, hover/focus/hidden/reduced-motion pauses, arrows, side-card selection, progress controls and responsive composition remain unchanged.

## Risks / Trade-offs

- **[Generated concept imagery requires final public-use confirmation]** → Record its provenance and licensing caveat in QA and completion reports; do not deploy.
- **[The long V4 target contains generated microcopy that is not product truth]** → Recreate the composition with reviewed, capability-accurate native UI text rather than rasterizing the whole page.
- **[Scroll geometry varies by viewport]** → Use one focus line, one animation-frame update per scroll burst, a deterministic initial state, a manual-selection hold, direct step controls, and a reduced-motion path.
- **[Theme initialization can add `.dark` globally]** → Use a self-contained landing root with explicit variables and color scheme so the public site remains stable without changing theme storage behavior.
- **[Anonymous intent could be misunderstood as saved]** → After explicit submission, state in the result panel that the intent remains in this browser session and requires login/invitation verification before generation or saving; never claim anonymous server persistence.
- **[Generated paper and blueprint raster assets require final public-use confirmation]** → Keep their ImageGen provenance in the I3 QA report, use them only in this local uncommitted task, and do not deploy.
- **[Generated cinematic role imagery requires final public-use confirmation]** → Keep ImageGen provenance in the I3 QA report, label the role cards as examples, use the assets only in this local uncommitted task, and do not deploy.
- **[R17 generated supporting roles require final public-use confirmation]** → Keep the rounded-fantasy, alpaca, headset-operator, senior-scientist, young-operator, and silver-fantasy ImageGen files local and uncommitted, record their paths and source briefs in R17 QA, and obtain an explicit licensing/publication decision before any deployment.
- **[R18 supplied-reference focal extraction requires public-use confirmation]** → Keep `hero-main-reference-r18.png` local and uncommitted, record its exact source coordinates and homography in QA, and do not publish or deploy it until the user confirms rights/authorization for public use.
- **[R23 generated showcase portraits require final public-use confirmation]** → Version the five source hashes and lossless WebP derivatives with their R23 QA provenance, label every card `品牌示例`, and obtain an explicit public-use decision before deployment.
- **[The live reference contains fabricated management metrics]** → Reproduce only its spatial and interaction model; render capability-accurate demo panels with no counts, performance claims, customer claims, or implied live operations.
