# Design

Advanced controls remain inside the existing Build section rail. Runtime uses the Provider catalogue for Cloud Agents and Edge credentials for Edge Agents. Media uses a canvas cropper without adding a dependency. Skills preserve all three backend stages and expose both Creator-global config and Agent-level overrides through a validated JSON editor.
