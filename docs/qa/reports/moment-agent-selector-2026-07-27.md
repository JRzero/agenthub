# 发布动态 Agent 选择器 QA

已完成发布动态页面的 Agent 选择体验调整，并通过功能与视觉验证。

- 选择器由全量平铺卡片改为紧凑选择字段和按需展开列表。
- 支持名称/编码搜索、可发布/全部筛选、滚动浏览和键盘关闭。
- 尚未发布 Agent 在“全部”中保留可见但禁止选择，并说明原因。
- 默认选择首个拥有平台当前版本的 Agent。
- 桌面端采用 Agent 选择与正文编辑并列布局；1024 px 视口无横向溢出。
- 未触发发布动作或创建后端 Moment 记录。
- 浏览器控制台无警告或错误。

完整对比结论见仓库根目录 `design-qa.md`。

final result: passed
