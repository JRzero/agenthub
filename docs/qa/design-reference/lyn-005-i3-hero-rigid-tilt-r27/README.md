# LYN-005-I3 R27 visual source

- Unique visual truth: `user-reference.png`, `2990 × 1468` pixels.
- The reference main card's visible long edges were measured at approximately `(1825, 82) → (2690, 150)` and `(1505, 1112) → (2470, 1195)`, yielding about `4.5°` and `4.9°` clockwise. Its visual center remains in the right-hand wall, overlapping the middle and lower rows.
- Background-card long edges cluster around roughly `3.5–5°` clockwise. Their small variations still read as one wall rather than random independent rotations.
- R27 follows the user's explicit rigid-plane constraint: the measured long-edge angle is recreated with pure `rotateZ`, not by reproducing any apparent quadrilateral shear from the flattened reference composite.
