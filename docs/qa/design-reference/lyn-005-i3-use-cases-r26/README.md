# LYN-005-I3 R26 visual source

- User issue screenshot: `user-reference.png` (`2880 × 586`, Retina-style wide crop of `03 / USE CASES`).
- Scope inferred from the annotation: preserve the established three-card layout, rounded dark treatment, title hierarchy, and bottom copy; replace only the three scenario images with the supplied higher-quality generated scenes.
- Supplied sources were inspected at their native sizes before conversion: `1635 × 962`, `1638 × 960`, and `1638 × 960`. Their 1.699–1.706 aspect ratios already match the rendered card ratio (`342.33 × 200`, about 1.712), so implementation uses proportional `cover` crop without any non-uniform transform.
